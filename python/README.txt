In order to run this code, you would need to install the gurobi package and acquire a free 
educational license.
Free education license can be acquired here: https://pypi.org/project/gurobipy/

Make sure to run this command on the terminal: pip install gurobipy

In order to generate the outputs into a file named outputs: python3 python/solve_all.py inputs outputs